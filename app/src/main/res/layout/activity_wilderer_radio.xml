<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_wilderer_radio"
    style="@style/general_style"
    tools:context="com.example.utente.musicadivina.WildererRadio">

    <ImageView
        android:id="@+id/album"
        style="@style/album_image"
        android:src="@drawable/wilderer"/>

    <TextView
        android:text="@string/welcome_three"
        style="@style/welcome_three_style"
        />

    <ImageView
        android:id="@+id/musicplayer"
        style="@style/media_player"
        android:src="@drawable/buttons"
        android:layout_below="@id/album"/>

    <TextView
        android:id="@+id/songname"
        style="@style/complete_work_style"
        android:text="@string/w_work_complete"
        android:layout_below="@id/musicplayer"/>

    <TextView
        android:id="@+id/downloadWilderer"
        style="@style/download_link"
        android:layout_below="@id/songname"
        android:text="@string/download"/>

</RelativeLayout>
